import { VisualPageAdmin } from "@/components/visual-page-admin";

export const metadata = { title: "Admin | Visual Pages" };

export default function PagesAdminPage() {
  return <VisualPageAdmin />;
}
